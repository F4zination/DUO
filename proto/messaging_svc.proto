syntax = "proto3";

package pb;

import "request.proto";
import "empty.proto";

option go_package = "github.com/duo/pb";


service MessagingService {
    rpc connect(Empty) returns (stream Request);
    rpc send(Request) returns (Empty);
}